# WARNING: This file is managed automatically and may be overwritten at any time.

name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

jobs:
  Commit:
    uses: schubergphilis-ep/github-terraform-workflows/.github/workflows/commit.yaml@10cd4b7ac9871ce51ec336dfeaf1b103de4a0af0 # v2.1.1
    permissions:
      contents: read
      pull-requests: write
    secrets: inherit

  Terraform:
    uses: schubergphilis-ep/github-terraform-workflows/.github/workflows/module-ci.yaml@10cd4b7ac9871ce51ec336dfeaf1b103de4a0af0 # v2.1.1
    permissions:
      contents: write
      security-events: write
    secrets: inherit
